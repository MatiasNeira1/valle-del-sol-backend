server:
  port: 8082

spring:
  application:
    name: ms-monitoreo
  datasource:
    url: jdbc:oracle:thin:@fxvgr0rd9mqxj20w_high
    username: ADMIN
    password: Bmcrojas2209
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    show-sql: true #esto hay que sacarlo dsp
    hibernate:
      ddl-auto: update

# 🚀 URL de tu otro microservicio
alerta:
  url: ${MS_ALERTA_URL:http://localhost:8081}

# 🛡️ Configuración del Circuit Breaker
resilience4j:
  circuitbreaker:
    instances:
      alertaCB:
        slidingWindowSize: 5 # Analiza las últimas 5 peticiones
        failureRateThreshold: 50 # Si el 50% falla, el circuito se ABRE
        waitDurationInOpenState: 10s # Espera 10 seg antes de intentar de nuevo
        permittedNumberOfCallsInHalfOpenState: 3